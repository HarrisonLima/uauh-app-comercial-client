import Text from "../../Text";

const TableEmpty = () => {
  return (
    <div className="table--empty">
      <Text fontText="Não há informações para serem exibidas!" variant="h3" />
    </div>
  );
};

export default TableEmpty;
