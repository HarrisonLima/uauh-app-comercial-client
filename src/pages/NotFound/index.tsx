import ModalNotFound from "../../components/Modals/NotFound";

const NotFound = () => {
  return (
    <main className="not-found">
      <ModalNotFound />
    </main>
  );
};

export default NotFound;
